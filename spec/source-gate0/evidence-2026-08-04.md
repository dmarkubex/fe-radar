# Gate 0 初始生产证据 — 2026-08-04

## 方法

- 环境：Portainer endpoint 3，生产 worker 容器。
- 临时出口：本机 Clash `10.106.15.158:7897`；仅本次复检，未写入生产 stack。
- 命令语义：生产 `verify-sources --include-disabled`，真实 fetcher + robots + ≥3 条门槛。
- 首轮单进程因唯一代理连续 3 次失败后熔断，后续结果被 `No healthy proxy` 污染；结论作废。
- 第二轮每个 source 使用独立 Node 进程，避免跨源熔断，共完成 62 行。
- 全程只读；未修改 sources、items 或配置。

## 结果

- 总行数：62
- 新闻源参加复检：41
- 技术通过：8
- 技术失败：33
- 类型/合规跳过：21（quotes/datapro/websearch 不计新闻 Gate；robots 禁止源保持跳过）

### 技术通过

|  ID | 信源                         | 启用 | 条数 | 内容抽样结论                                     |
| --: | ---------------------------- | :--: | ---: | ------------------------------------------------ |
|   6 | 中关村储能产业技术联盟 CNESA |  否  |   12 | 高相关；但 list 无真实发布时间，按 0040 继续禁用 |
|   5 | 中国电器工业协会             |  是  |   21 | 行业相关                                         |
|   9 | 巨潮资讯                     |  是  |   30 | 原始流很宽，必须依靠公司/实体过滤                |
|  11 | 深交所披露                   |  是  |   30 | 公告流，必须依靠证券代码/实体过滤                |
|  20 | 中国电力新闻网               |  否  |   10 | 电力政策/供需高度相关，可进入激活候选            |
|  25 | 国际能源网                   |  否  |   70 | 能源、电缆材料、项目相关，可进入激活候选         |
|  33 | 凤凰财经 能源                |  是  |  169 | 宽口径，必须保留 keywordFilter                   |
|  29 | 财联社 能源                  |  是  |   25 | 能源主题，仍需 keywordFilter                     |

### 主要失败类型

- 官方 adapter/接口：上交所 0 条；发改委、能源局、工信部经代理仍失败。
- 失效 URL：CPIA、CWEA、中国电网、储能网、网易、钛媒体等 404/405。
- 选择器/页面：北极星系列 `.news-list` 超时或 page.goto 超时；legacy RSS 404/解析失败。
- 合规禁止：中电联、知乎、搜狗微信、索比、雪球等保持禁止或跳过。
- 配置缺失：Firecrawl-C1 缺运行时 key，且另有裸“远东”误配风险，继续禁用。

## 初始裁决

Gate 0 = **FAIL**。当前信源组合未满足六域各 2 个独立健康源，且无七日抓取尝试历史可证明 ≥90% 成功率。

## 招标专项增补证据

- 用户于 2026-08-04 明确将大型企业电线、电缆、储能相关招标列为重要信息，已纳入 Gate 0 硬验收。
- 生产 worker 网络直连 `https://www.bidding.csg.cn/ggxx/index.jhtml` 返回 HTTP 200，列表包含采购人、公告标题、原文链接和真实公告日期；可作为南方电网官方招采首批候选。
- 生产 worker 通过一次只读浏览器网络观察定位到国网公开 POST 接口 `https://ecp.sgcc.com.cn/ecp2.0/ecpwcmcore/index/noteList`；接口无需登录，按公告栏目和关键词返回采购单位、标题、公告日期、公告类型和 `firstPageDocId`。
- 国网接口生产直连抽样：招标公告关键词“电缆”返回 146 条（首批 20 条）、“储能”返回 3 条；采购公告“电缆”返回 77 条、“储能”返回 21 条；候选人及结果栏目也能独立查询。robots.txt 为 404（未发现明确禁止规则），但仍须经 worker robots policy 与 adapter allowlist 烟测后才计入健康源。
- 常态抓取不使用 Playwright：国网应使用严格 allowlist 的 JSON adapter；浏览器只用于本次发现公开接口。

## 大型企业招采下一批验证

- 中国电建新平台 `bid.powerchina.cn`：公开 JSON 接口 HTTP 200。关键词“电缆”返回招采 3460、候选公示 66、成交公示 3196；“储能”分别返回 2226、191、2242。抽样记录发布时间为 2026-08-03/04，含公告类别和官方原文 PDF。
- 国家能源集团“国能 e 招”：`https://www.chnenergybidding.com.cn/bidweb/` HTTP 200，约 638 KB；首页抽样包含 2026-08-03/04 的储能招标、中标候选和中标结果。列表只显示月日，但官方原文 URL 路径含 `YYYYMMDD`，adapter 从 URL 提取完整日期，不用抓取时间补齐。
- 中国能建旧入口 `ec.ceec.net.cn/HomeInfo/ProjectList.aspx` HTTP 200，但当前公开页未检出电缆/储能条目；不满足最少 3 条准入条件，本批不落库。
- 中国电建旧入口 `ec.powerchina.cn/index.html` 虽 HTTP 200，但样本内容停留在 2025 年；不作为新源，新 adapter 仅使用 2026 年新平台公开接口。
- `robots.txt` 未发现针对中国电建新接口或国能 e 招的明确 `Disallow`；运行时仍统一经过 `fetchTextWithPolicy()` 检查，不以代理绕过规则。
- 2026-08-04 已将 migration 0047 的 5 条候选写入生产 `sources`：ID 696–700，全部 `enabled=false`；其中 4 个招采平台均标记 `signalKinds=tender`。生产查询确认 `https://www.fe-cable.com/news.html` 为 0 行，未新增远东电缆。
