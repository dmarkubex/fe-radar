# Gate 0 证据补录 — 2026-08-15（信源激活批次）

执行通道：Portainer endpoint 3 websocket exec（新工具 `deploy/scripts/portainer-exec.mjs`），生产 worker / postgres 容器内只读烟测 + admin 级 config 调整。所有变更直接落生产 DB（admin 行为，非 migration；后续 reseed 不覆盖，受 0034 admin_touched 保护）。

## 本批裁决汇总

|  ID | 信源                       | 动作                                    |   技术烟测 | 相关率证据                                                                              | 最终状态                                                           |
| --: | -------------------------- | --------------------------------------- | ---------: | --------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| 698 | 南方电网公开招采           | listUrl 改搜索端点 + 启用               | PASS 10 条 | 新入库 5/5 相关（100%），全为电缆招标                                                   | **已启用**                                                         |
|  20 | 中国电力新闻网             | listUrl 改电力频道 + 新 selector + 启用 | PASS 15 条 | ~70%（频道仍混煤炭/宏观，预筛正确兜底）                                                 | **已启用（有条件）**                                               |
| 718 | LS Cable & System 官方新闻 | 启用                                    | PASS 15 条 | 当日新条目 3/3（100%）；存量 14/18（77.8%）系韩文预筛漏判，人工复核内容几乎全为电缆产业 | **已启用**                                                         |
| 719 | Prysmian 官方新闻          | keywordFilter + 启用                    | PASS 10 条 | 过滤后当日新条目 1/1 相关；过滤规则人工推演 7/7                                         | **已启用**                                                         |
| 720 | CATL 官方新闻              | keywordFilter + 启用                    |  PASS 9 条 | 过滤后当日新条目 1/1 相关；过滤规则人工推演 5/5                                         | **已启用**                                                         |
| 712 | 起帆电缆官方新闻           | 复测                                    |  PASS 5 条 | 最新内容仍 2026-04-20（117 天前），超 90 天窗口                                         | **保持禁用**（无新内容）                                           |
|  25 | 国际能源网                 | 复测                                    |       FAIL | 列表页只有相对时间（"1天前"），无绝对日期                                               | **保持禁用**，需 `parsePublishedAt` 支持相对日期（代码改动，下批） |

## 配置变更明细（生产 DB admin 变更）

1. **698 南网**：`config.listUrl` 由 `…/zbcg/index.jhtml` 改为搜索端点
   `https://www.bidding.csg.cn/dbsearch.jspx?channelId=309&types=&org=&q=电缆`。
   原因：首页聚合全品类公告，keywordFilter 后 0 条触发 `FETCH_HTML_EMPTY`；搜索端点返回相同 `.List2 ul > li` 结构且按关键词收敛。selector 未变。
   遗留：储能类目需另起一行 source（q=储能），走新 migration，本批未做。
2. **20 中国电力新闻网**：`listUrl` 改 `https://www.cpnn.com.cn/news/dianli2023/`（电力频道），
   selectors 改 `item=.cpnnlist_l ul > li / title=a.fl / link=a.fl / date=b.fr`（绝对日期 `2026-08-13`）。
3. **719 Prysmian**：`keywordFilter` = cable/energy/grid/submarine/offshore/transmission/electrical/data center/fiber/HVDC/power（大小写变体），滤掉 CFO 任命、回购、合规公告等公司噪声。
4. **720 CATL**：`keywordFilter` = battery/storage/ESS/charging/energy/sodium（大小写变体），滤掉运河、碳交易所、颁奖等非电池主业新闻。

## 复测方法

- 技术烟测：`node dist/apps/worker/src/scripts/verify-sources.js --include-disabled --source-id <id> --json`（生产 worker 容器内，走真实 fetcher + robots + UA 策略）。
- 相关率：手动向 BullMQ `fe-fetch` 队列入队 `fetch-source {sourceId}`（不等 6h 周期），真实入库后经 Qwen 预筛，查 `item_analysis.is_industry_related` 统计。

## 第五批国际信源（migration 0062，2026-08-15 深夜批次）

新增 7 个国际源（`packages/db/migrations/0062_source_gate0_batch5_international.sql`，默认禁用格式 + 同日 admin 落库启用达标者）。全部经生产 worker 网络探测（robots + 真实 fetcher + 真实日期）→ verify-sources 烟测全 PASS → 入队真实入库 + Qwen 预筛相关率复测：

|  ID | 信源                         | 类型 | 烟测 |                                                                                                                                                                   相关率 | 裁决                                               |
| --: | ---------------------------- | ---- | ---: | -----------------------------------------------------------------------------------------------------------------------------------------------------------------------: | -------------------------------------------------- |
| 725 | ESS News（国际储能）         | rss  |   10 |                                                                                                                                                         10/10 = **100%** | 启用                                               |
| 726 | Utility Dive（美国公用事业） | rss  |   10 |                                                                                                    7/10 = 70%，但 3 条误判均为燃气电厂/地热等电力内容（人工复核 ~10/10） | 启用（人工复核）                                   |
| 727 | pv magazine（光伏与储能）    | rss  |   10 |                                                                                                            7/10 = 70%，误判含 PV 清洁机器人、Neom 绿氢（人工复核 ~9/10） | 启用（人工复核）                                   |
| 728 | Canary Media（清洁能源）     | rss  |  100 |                                                                                                            53/100 = **53%**，消费/生活类真噪声且单批 100 条 quota 消耗大 | **禁用**，待 rss fetcher 支持 keywordFilter 后复检 |
| 729 | Europacable 欧洲电缆协会     | rss  |   10 |                                                                                                                     3/3 = **100%**（低频源，最新 2026-06-05 仍在窗口内） | 启用                                               |
| 730 | EIA Today in Energy          | html |   15 | 5/15 = 33%，不相关项全为油气/LNG（正确排除）；加 keywordFilter（electricity/power/battery/storage/grid/solar/wind/renewable/nuclear/hydrogen/peak load）后推演 5/6 ≈ 83% | 启用（收窄后达标）                                 |
| 731 | Wärtsilä 瓦锡兰              | html |    6 |                                                                                              4/6 = 66.7%，2 条船舶维修协议为真噪声（正确排除），keywordFilter 已前置收窄 | 启用（有条件，同 20 号源列入 7 日观察）            |

探测失败未落库：energy-storage.news（Cloudflare 403，realUA 亦败，需 playwright 通道）、EIA RSS（robots 禁 /rss/，改 HTML 页合规通过）、Fluence / DCD（Cloudflare 挑战）、Powin（官网跳转 flexgen 404）、NKT（连接超时）、BYD（Vue 动态渲染，CMS API 在 cms-prod.byd.com/es/search，需专用 adapter）。

## 第六批通道开发（tasks-batch6.md，2026-08-15/16）

- **T-G5-01 rss keywordFilter**：`rss.ts` 补过滤逻辑（语义镜像 html.ts），`FETCH_RSS_EMPTY` 抛错。单测 3 例通过。
- **T-G5-02 prefilter 韩文词**：`prefilter.ts` 追加韩文行业词段（케이블/해저케이블/전력/에너지저장/배터리/입찰 等），解决 718 LS 韩文漏判。
- **T-G5-03 相对日期**：`parsePublishedAt` 支持"N分钟/小时/天前"与"N hours ago"，>365 天返回 null。单测覆盖。
- **T-G5-04 BYD**：**取消**。CMS 接口可达但 en 站停更 13 个月、cn 站最新 2023-07，无内容价值。
- **T-G5-05a playwright dateSelector/dateAttribute**：已落地（声明式，无 T-SEC-03 风险）；配 dateSelector 且日期解析失败的条目丢弃，未配保持兜底不变。CF 站点探测（05b）待部署后进行。
- **T-G5-06 0063 南网储能招采（ID 732）**：已落库并启用——verify-sources PASS 14 条；真实入库 14 条、预筛相关 13 条 = **92.9%** 达标。
- **T-G5-07 部署**：代码（rss filter / 韩文 prompt / 相对日期 / playwright dateSelector）需新 worker 镜像，本机无 Docker 环境，**暂缓部署**。部署后待办：① Canary（728）加 keywordFilter 复检；② 25 国际能源网 date 改 `i.fr` 启用（标题会混入"N天前"文本；整串锚定后混排会解析为 null，见下方遗留，勿当成选择器配错）；③ CF 站点（energy-storage.news/Fluence/DCD）playwright 探测，过则 0064 seed。
- 验收：worker 66 文件 / 558 测试全绿；tsc worker + llm 0 error。

## 遗留与风险

- **20 号源相关率 ~70% 低于 80% 闸门**：误判项均为真噪声（煤炭/乡村振兴/APEC 研修班），预筛已正确丢弃，时间线不受污染，代价仅是少量预筛 quota。暂保留启用，7 日稳定性观测后裁决，不达标则回退 `enabled=false`。
- **718 LS 韩文预筛漏判**：约 4/18 条韩文电缆新闻被预筛误判不相关而丢弃（覆盖损失，非噪声）。建议下一批在 `packages/llm/src/prompts/prefilter.ts` 补韩文行业词（케이블/해저케이블/전력망 等），属代码改动。
- **25 国际能源网**：相对日期解析已落地，但启用仍需单独处理。列表页标题会混入"N天前"文本；整串锚定上线后，若日期选择器捕获到标题+日期混排，`parsePublishedAt` 会返回 `null`（fail-closed：条目因日期缺失被丢弃，不会把陈旧内容当成"现在"）。启用时必须把日期选择器收窄到只捕获日期本身，或对该信源单独处理——不要先当成选择器配置错误排查。依据：`.ai/reviews/2026-08-17-t-g5-batch6-fix5-reverify-review.md`。
- **rss fetcher 不支持 keywordFilter**（`rss.ts` 无此逻辑）；718 如需收窄只能改 html 抓取或等预筛增强。
- 所有新启用源进入 7 日稳定性观测（单源成功率 ≥90%、单源产出占比 ≤30%）。
