# S7 — mirofish 出站 payload 未全量脱敏（High，X-2）

- executor: `glm`

## ⚠ 本任务禁止 git 提交

不要 `git commit` / `add` / `stash` / `reset` / `checkout <文件>`。改动留工作区。
进度**追加**到根目录 `handoff.md` 末尾（小写），不要改已有内容。

## 已观测缺陷

`apps/web/lib/api/mirofish.ts:178` / `:248` —— 只 scrub 了文档正文和三个顶层字段。
**`documents[].source_url` 与整个 `metadata` 原样外发**；scrub 的 audit 结果也被丢弃。

**失败场景**：信源名为 `联系人13800138000`，或原文地址为 `http://10.0.0.5/report` 时，
正文虽已脱敏，但手机号 / 内网地址仍从 `metadata.source_name` / `metadata.source_url`
原样发送到使用公网 LLM 的 MiroFish。

这直接违反项目硬约束：**公网 LLM 调用前必经 `packages/core/scrubber.ts`（脱敏 + audit log；
命中 PII 阈值跳过 LLM）**，以及 **不拉 / 不存用户手机号**。

## 要做的事

1. 对出站 payload 中**所有字符串字段递归 scrub**（含嵌套对象与数组），
   或删掉非必要的结构化字段。优先递归，不要靠逐字段列举——列举法漏一个就等于没做。
2. **汇总 block 判定**：任一字段命中 PII 阈值 → 整个请求按项目约定跳过 LLM，
   不要出现"正文被拦、metadata 照发"。
3. **记录 audit**，不要丢弃 scrub 返回的审计结果（这是硬约束的一半）。

## 允许修改的文件（清单外即越界）

```
apps/web/lib/api/mirofish.ts
apps/web/lib/api/__tests__/mirofish.test.ts
apps/web/app/api/items/[id]/mirofish/route.ts
handoff.md
```

## 必须新增的测试

1. `metadata.source_name` 含手机号 → 出站 payload 中该字段已脱敏（断言原始号码子串**不出现**在最终 payload 里）。
2. `documents[].source_url` 为 `http://10.0.0.5/report` → 内网地址已脱敏。
3. 嵌套结构（`metadata.nested.deep` 之类）也被覆盖——防"只处理了第一层"。
4. 命中 block 阈值时**整个请求**不调 LLM（断言 LLM mock 未被调用）。
5. audit 结果被记录（断言 logger / audit 落点被调用）。

**断言方式要求**：用"原始敏感子串不出现在序列化后的完整 payload 中"来断言，
不要只断言某个字段被改过——前者能覆盖你没想到的字段，后者不能。

## 机械检查（结果写进 handoff.md）

1. `grep -rn "withScrubber\|scrubber" apps/web/lib/api/mirofish.ts` —— 列出所有 scrub 调用点，
   确认出站前的每条路径都经过。
2. **镜像校验**：`grep -rn "mirofish" --include=*.ts apps packages | grep -v node_modules | grep -v __tests__`
   —— 确认没有第二条绕过本文件直接调 MiroFish 的路径。
3. **回代验算**：把上方失败场景的两个具体值（`联系人13800138000`、`http://10.0.0.5/report`）
   代入修复后的逻辑，把最终出站 payload 的序列化结果抄进 handoff.md，确认两个原始值都不出现。

## 验收命令

```bash
cd /Volumes/SD/AI-Timeline-web/apps/web && npx vitest run
cd /Volumes/SD/AI-Timeline-web/apps/web && npx tsc --noEmit -p tsconfig.json
```

第一条必须 0 failed，原始输出附在最终回复末尾。
