先读内核状态与相关源码，再按 T18 范围只改 `briefing-push` 及其测试。继续读测试后半段和参考 mock 模式，并核对 `isBusinessDay` 的接口。开始改 `briefing-push.ts`，并同步调整测试里的 select 顺序与 CAS mock。接下来改测试：加强 CAS mock，并调整 select 序列顺序。恢复 `beforeEach` 里的 `isBusinessDay` mock，并核对是否还有未改的 select 顺序。验收已绿。按任务要求做回代验算：先确认旧逻辑下相关测试会红。回代验算已确认（旧逻辑会红、修复后会绿）。正在把进度追加到 `handoff.md`。## T18 完成（未 commit，改动留工作区）

### 修复

`runBriefingPush` 改为：

1. **先加载** `briefing`（找不到仍返回 `briefing_not_found`）
2. **再做** 工作日判断
   - **manual** → `briefing.briefingDate`
   - **scheduled** → `options.reportDate`（不变）
3. 删除两处失实 **F8** 注释

周五推送失败、周六人工重推时，不再因墙钟“周末”被静默 `not_business_day`。

### 测试加固

- CAS mock 真正解析 WHERE 的 `id` / `push_status` / `attempt_count`
- 新增 stale cursor 测试：reclaim 后旧 `expectedAttemptCount` finalize → **0 行**
- 新增 manual 工作日门回代两条（宿主周末 vs 简报工作日；宿主工作日 vs 简报节假日）

### 回代验算（先红后绿）

| 探针             | 人为回退旧逻辑                     | 结果                                   | 当前实现 |
| ---------------- | ---------------------------------- | -------------------------------------- | -------- |
| manual 工作日门  | manual 改回 `new Date()`           | **2 fail**（收到 `Date` 而非简报日期） | **绿**   |
| CAS attemptCount | finalize WHERE 删掉 `attemptCount` | **1 fail**（旧游标误返回 true）        | **绿**   |

### 范围

仅改：

- `apps/worker/src/jobs/briefing-push.ts`
- `apps/worker/src/jobs/__tests__/briefing-push.test.ts`
- `handoff.md`（追加进度；被 `.gitignore` 忽略）

### 后续参考（未越界）

`daily-push.test.ts:235` 同样是 `where: (_where) =>` 忽略 CAS 条件，可镜像加固。

### 机械检查

```
grep -n "F8" apps/worker/src/jobs/briefing-push.ts  → 无匹配
isBusinessDay: scheduled → options.reportDate; manual → briefing.briefingDate
```

---

### 验收命令输出

```text
 RUN  v4.1.5 /Volumes/SD/AI-Timeline-web/apps/worker

 Test Files  1 passed (1)
      Tests  26 passed (26)
   Start at  21:47:43
   Duration  214ms (transform 82ms, setup 0ms, import 115ms, tests 30ms, environment 0ms)
```

```text
npx tsc --noEmit -p apps/worker
（exit 0，无输出）
```
