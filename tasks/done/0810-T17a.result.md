我先按内核要求读取权威规格与现状代码，再只在允许的 6 个文件内实现 CAS 化、队列判别联合与人工重推路由。接下来读当前 `daily-push`、bootstrap、queues 与现有 repush 参照实现。规格已对齐。先把 restate 写入 `handoff.md`，再实现允许范围内的 6 个文件。接下来写 `daily-push` 的 CAS/并发/stale 核心测试。并发测试的 select mock 会交错失败，改成用 `AsyncLocalStorage` 给每次调用独立序号。## T17a 完成（改动留工作区，未 commit）

按规格 `.ai/reviews/2026-08-10-dp-fix-spec-final.md` 落地，范围仅 6 个允许文件 + `handoff.md` 追加。

### 实现要点

| 文件                                     | 变更                                                                                                                                              |
| ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| `daily-push.ts`                          | CAS claim-first：INSERT → 授权 UPDATE（`attempt_count+1` + DB 续租）→ 终态 CAS；stale 120s；failed 不自动重试；`runManualDailyPush`；全部成功早退 |
| `queues.ts`                              | `BriefingPushJob` 判别联合 `minute-tick` / `briefing-repush` / `daily-repush` + legacy `{briefingId}` 兼容                                        |
| `bootstrap.ts`                           | 分钟 tick **只采样一次** `now` → `runScheduledDailyPush({ now })`；新增 daily-repush 分支                                                         |
| `.../daily/[reportDate]/repush/route.ts` | fresh-admin；400/404/422/202；入队 `{ kind:"daily-repush", reportDate, trigger:"manual" }`                                                        |

### 机械检查

- **无** `pushedAt` JS 等值匹配；仅 `sql\`now()\`` 写入 + DB 侧 stale 年龄
- 授权/reclaim 为**单条** UPDATE 完成所有权 + 计数 + 续租
- 无新 migration

### 回代验算（单测）

- 2 / 20 并发 claim → **1** webhook / **1** 行
- 119999ms 不 stale；120000ms → `STALE_PENDING_DELIVERY_UNKNOWN`
- failed 后 10 tick → webhook 增量 **0**
- manual reclaim `attemptCount` 3→**4** 后 succeeded

### 下一步（非本任务）

- **T17b**：镜像改 `briefing-push.ts`（接 `now`、manual reclaim）
- scheduler / briefing repush 路由改显式 `kind` / `trigger`

---

### 验收命令输出

```text
cd apps/worker && npx vitest run src/jobs/__tests__/daily-push.test.ts 2>&1
 Test Files  1 passed (1)
      Tests  23 passed (23)

cd apps/web && npx vitest run "app/api/briefing/daily/[reportDate]/repush/__tests__/route.test.ts" 2>&1
 Test Files  1 passed (1)
      Tests  9 passed (9)

cd /Volumes/SD/AI-Timeline-web && npx tsc --noEmit -p apps/worker 2>&1
(exit 0, no output)

cd /Volumes/SD/AI-Timeline-web && npx tsc --noEmit -p apps/web 2>&1
(exit 0, no output)
```
