按 T17b 要求执行：先读权威规格与 T17a 的 `daily-push.ts` 参照实现，再按允许范围镜像 CAS 化。注意本任务禁止任何 git 写操作。继续读取测试、bootstrap、repush 路由与 UI 相关文件。正在实现 `briefing-push.ts` 的完整 CAS 化重写。接着改 bootstrap、repush 路由和 UI 文案。正在重写测试，并新建 repush 路由测试。检查 `runBriefingPush` 的其它调用点，并跑验收测试。失败原因是 `loadExistingPush` 的 select 序列被 `baseUrl` 配置查询占用。正在修正这三条测试。加临时调试看清失败路径。根因是 `setupSelectSequence` 在序列耗尽后返回空数组，没有回落到 store。## T17b 完成（未 commit）

镜像 T17a 的 CAS claim-first 已落到铜锂简报推送侧；改动仅在允许的 6 个文件 + `handoff.md`，**无 git 写操作**。

### 改动摘要

| 文件                             | 内容                                                                                                                                                                                             |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `briefing-push.ts`               | CAS：`insertClaim` / `authorize` / `reclaim` / `markStale` / `finalize` / `sendWithCas`；业务键 `(briefingId, targetId)`；scheduled/manual 分流；stale=120s；全部成功早退；单一时钟 `reportDate` |
| `bootstrap.ts`                   | `runScheduledBriefingPush({ now })` 同 tick；手工 job `mode: "manual"`                                                                                                                           |
| `repush/route.ts`                | 载荷 `{ kind: "briefing-repush", trigger: "manual", ... }`                                                                                                                                       |
| `repush/__tests__/route.test.ts` | 新建 6 用例                                                                                                                                                                                      |
| `schedule-form.tsx`              | 「两张独立卡片、各自发送时间」                                                                                                                                                                   |
| `briefing-push.test.ts`          | 并发 / stale / failed×10 tick / 单时钟回代                                                                                                                                                       |

### 关键行为变化

- **修复前**：`failed` 每分钟 tick 会重试到 23:59
- **修复后**：scheduled 路径 webhook 增量 **0**；仅 manual reclaim
- **并发**：同 `(briefingId, targetId)` 恰好 1 次 webhook
- **时钟**：`MIDNIGHT_TICK` → `reportDate=2026-08-04`，与工作日判断同值

---

### 验收命令输出（原样）

```
cd apps/worker && npx vitest run src/jobs/__tests__/briefing-push.test.ts 2>&1
```

```
 Test Files  1 passed (1)
      Tests  23 passed (23)
```

```
cd apps/web && npx vitest run "app/api/briefing/[id]/repush/__tests__/route.test.ts" 2>&1
```

```
 Test Files  1 passed (1)
      Tests  6 passed (6)
```

```
npx tsc --noEmit -p apps/worker 2>&1  → exit=0
npx tsc --noEmit -p apps/web 2>&1     → exit=0
```
