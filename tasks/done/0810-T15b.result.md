按 T15b 约束执行：只改 `http.ts` 与测试，先读现状再修 allowlist 跳过代理逻辑。先确认 `isInternalAllowlisted` 的签名，以及代理重试路径是否也要跳过。先只加复现测试并跑红，再改 `http.ts` 跳过 allowlist 主机的代理。修复前测试已红。正在改 `http.ts`，让 allowlist 主机跳过代理池（含重试）。验收通过。正在把进度追加到 `handoff.md` 末尾。## T15b 完成

### 问题

`fetchTextWithPolicy` 无条件 `proxyPool.acquire()`，`FETCH_INTERNAL_ALLOWLIST` 主机（如 `rsshub:1200`）在代理池开启时仍被塞进外部 `ProxyAgent`，外部代理解不了 Docker 内网 hostname，内部信源会持续断供。

### 修法

在 `apps/worker/src/fetchers/http.ts` 中：

1. 解析 URL hostname，调用已导入的 `isInternalAllowlisted`
2. 命中则 `skipProxyForInternal = true`，**首次与重试路径均不 acquire**，`proxy` 恒为 `undefined`
3. `buildDispatcher(undefined, …)` 自然直连（仍挂 SSRF lookup 守卫）

重定向到 allowlist 内部服务的代理选择**未做**（任务明确不要求）。

### 回代验算

| 阶段       | 结果                                                                                        |
| ---------- | ------------------------------------------------------------------------------------------- |
| **修复前** | 新测 **红**：`expected "vi.fn()" to not be called … been called 1 times`（`mocks.acquire`） |
| **修复后** | **12/12 绿**；`tsc --noEmit -p apps/worker` 0 error                                         |

### 机械检查

- `isInternalAllowlisted` 在 **L244**，`proxyPool.acquire()` 在 **L249**（之后）；L315/L348 重试同样受门控
- 其它 fetcher / robots 未新增对该函数的调用

### 范围

仅改：

- `apps/worker/src/fetchers/http.ts`
- `apps/worker/src/fetchers/__tests__/http.test.ts`
- `handoff.md`（进度追加）

**未 commit**（任务禁止 git 写操作）。

---

### 验收命令输出（原样）

```text
cd apps/worker && npx vitest run src/fetchers/__tests__/http.test.ts 2>&1

 RUN  v4.1.5 /Volumes/SD/AI-Timeline-web/apps/worker

 Test Files  1 passed (1)
      Tests  12 passed (12)
   Start at  19:45:01
   Duration  287ms (transform 80ms, setup 0ms, import 196ms, tests 7ms, environment 0ms)
```

```text
cd /Volumes/SD/AI-Timeline-web && npx tsc --noEmit -p apps/worker 2>&1
(exit 0, no output)
```
