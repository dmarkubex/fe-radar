# T18 — briefing-push.ts：人工重推工作日判断用错日期（High，两位评审独立收敛）

- executor: `grok`
- 基线: `f31d66e2419fa4b5bec3130eb3b76f2e7bc0f07c`（此后工作区已应用 T15a-e/T16/T17a/T17b）

## ⚠ 本任务禁止 git 写操作

不要 `git commit` / `add` / `stash` / `reset` / `checkout <文件>`。改动留工作区。
进度**追加**到 `handoff.md` 末尾（小写）。跑命令用 `2>&1`，不要留临时文件。
**不要连接任何真实数据库或生产环境**，本任务只改仓库文件。

## 允许修改范围（补集法核对，不得越界）

```
apps/worker/src/jobs/briefing-push.ts
apps/worker/src/jobs/__tests__/briefing-push.test.ts
```

只允许改这两个文件。回收时用 `git status --porcelain` + `git diff --name-only HEAD`
组合核对，出现清单之外的文件一律打回。

## 背景（第十轮闭环复核，codex 与 kimi 独立发现同一条 High）

`.ai/reviews/2026-08-10-t16-t17-arbitration.md` 完整裁决记录。核心问题：

`runBriefingPush`（约 :648-663 附近）的 manual 模式工作日判断用
`isBusinessDay(new Date(), holidaySet)`——即请求处理时刻的墙钟"今天"，
不是简报自身的日期。代码里有两处注释援引"F8"作为"这是已知技术债、本轮
不修"的依据——**这两处注释是错的，请直接删除**：`.ai/reviews/2026-08-10-dp-fix-spec-final.md`
（本项目当前唯一权威规格）第 5 节明确写着"manual repush 使用简报自身日期
或请求中的 `reportDate`，不得混用请求处理时的'今天'"，规格全文不存在
"F8"这个编号——那条注释是上一个任务单（T17b）里主会话本人误引了一条
针对**旧代码**（当时 briefing 侧还有"失败自动重试到 23:59"兜底）的过时
评估，写任务单时的失误，不是本该保留的设计决策。

**失败场景**：周五 17:00 铜锂日报推送失败（`pushStatus='failed'`）。因为
T17b 已经把"失败自动重试"整个去掉，唯一的恢复路径是人工重推。周六运维
点击重推，API 返回 `202`（前端显示"已提交"），但 worker 侧用**周六**这个
日期判断工作日 → `false` → `skipped: not_business_day`，零发送、零告警。
`daily-push.ts` 侧的 `runManualDailyPush` 已经正确使用请求携带的
`reportDate` 判定，两侧本该镜像，这里不对称。

## 修法

1. `runBriefingPush` 内部，holiday 检查目前在**加载 `briefing` 记录之前**
   执行（用不到 `briefing.briefingDate`，只能用墙钟）。改成：先加载
   `briefing`（`briefing` 查不到时按现状逻辑返回 `briefing_not_found`），
   再做 holiday 检查——manual 模式下用 `briefing.briefingDate` 判定，
   scheduled 模式继续用 `options.reportDate`（不变）。
2. 删除代码里两处援引"F8"的注释（`RunBriefingPushOptions.reportDate` 字段
   注释、以及 holiday 检查那段的注释），改成准确描述当前行为的注释
   （不需要再提"技术债"，因为这次是真的修了，不是继续搁置）。
3. `emptyResult({briefingId, skipped:true, reason:"not_business_day"})` 分支
   如果因为改了检查顺序需要调整返回时机，保持现有的 `reason` 字符串不变
   （下游/测试可能依赖这个值）。

## 顺手修（Medium，同一批文件，不必单独起任务）

`briefing-push.test.ts` 里 CAS 并发测试用的假 `sql` 标签函数，授权/终态
UPDATE 的 mock 目前忽略了 `_where` 参数，只对"当前唯一一条 pending 行"
生效——这意味着如果生产代码的 `id`/`attemptCount` CAS 条件被误删，现有
测试大概率仍然全绿，测不出这类回归。请让 mock 真的解析/校验 UPDATE 语句
携带的 `id`/`pushStatus`/`attemptCount` 条件（参照
`packages/db/src/__tests__/purge-legacy-viewer-branch-b.test.ts` 或
`packages/db/src/__tests__/seed-admin.test.ts` 里"假 sql 标签函数检查真实
拼出来的 SQL 文本"这个已经在本项目验证过的模式），并新增一条测试：
"reclaim 之后，持有旧 `expectedAttemptCount` 游标的 owner 尝试 finalize，
必须影响 0 行"（这条测试要求能在"CAS 条件被误删"时变红——先跑一遍确认
删掉 `attemptCount` 条件后这条新测试真的会红，再确认当前实现下会绿，
两次结果都写进最终回复）。

**如果 `daily-push.test.ts` 里同样存在这个 mock 忽略 `_where` 的问题，
不在本任务范围内修（那个文件不在本次允许修改范围），只在最终回复里提一句
供后续参考即可，不要越界去改它。**

## 强制机械检查

1. `grep -n "F8" apps/worker/src/jobs/briefing-push.ts` 确认两处失实引用
   都删干净了。
2. `grep -n "isBusinessDay" apps/worker/src/jobs/briefing-push.ts` 确认
   manual 分支现在用的是 `briefing.briefingDate`，scheduled 分支用的还是
   `options.reportDate`，两者没有被搞反。
3. 回代验算（强制）：构造"宿主机真实日期是周末，简报日期是工作日"的场景，
   manual 重推应该正常发送（不被工作日门拦）；反过来"宿主机是工作日、
   简报日期是节假日"，manual 重推应该被正确拦下。**先确认修复前这条测试
   会红**（旧逻辑用墙钟判断，宿主日期是周末时不管简报日期是什么都会拦），
   两次结果都写进最终回复。

## 验收命令

```bash
cd apps/worker && npx vitest run src/jobs/__tests__/briefing-push.test.ts 2>&1
cd /Volumes/SD/AI-Timeline-web && npx tsc --noEmit -p apps/worker 2>&1
```
