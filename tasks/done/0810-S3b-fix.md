# S3b-fix — 更新 5 个 route 测试的过时 mock（S3b 收尾）

- executor: `grok`
- 前置：S3b 已完成（`requireFreshViewer` 删除了"缺 tokenVersion 即放行"的早返回）

## ⚠ 本任务禁止 git 提交

**不要 `git commit` / `git add` / `git stash` / `git reset` / `git checkout <文件>`。**
改动留在工作区。进度**追加**到根目录 `handoff.md` 末尾（小写文件名），不要修改已有内容。

## 背景（不要"修复"生产代码，生产代码是对的）

S3b 把 `apps/web/lib/auth/token-freshness.ts` 里
"JWT 缺 `tokenVersion` claim 就跳过查库直接放行"的早返回删掉了，
改为交给 `verifyTokenFreshness` 统一判 stale。**这是有意的安全修复，行为正确。**

后果：5 个 route 测试把 `getRequestUser` mock 成 `{ id: 1, role: "viewer" }`
（**不含 `tokenVersion`**），于是这些请求现在正确地被判 stale → 401，
而测试仍断言 200/400 → 24 个用例失败。

**失败的是测试的 mock 过时了，不是生产代码有问题。**

实测现状：

```
Test Files  5 failed | 48 passed (53)
Tests  24 failed | 382 passed (406)

AssertionError: expected 401 to be 200
AssertionError: expected 401 to be 400
```

## 要做的事

给下列 5 个测试文件里所有 `mockGetRequestUser.mockResolvedValue({...})`
（及等价的用户 mock）补上 `tokenVersion` 字段，使其代表**一个正常在职用户**。

同时确认这些测试文件对 `verifyTokenFreshness` 的处理：如果它们没有 mock 该函数，
就需要 mock 成"新鲜"（valid），否则会真去查库。**优先选择 mock `verifyTokenFreshness` 返回
valid**，这样测试意图（验证 route 的参数透传与 RBAC）保持不变，不被认证细节污染。

## 允许修改的文件（逐条枚举，改此清单之外的任何文件即为越界）

```
apps/web/app/api/timeline/__tests__/timeline-route.test.ts
apps/web/app/api/search/__tests__/search-route.test.ts
apps/web/app/api/items/[id]/__tests__/items-api.test.ts
apps/web/app/api/quotes/latest/__tests__/quotes-latest-route.test.ts
apps/web/app/api/briefing/[id]/__tests__/route.test.ts
handoff.md
```

**严禁修改任何生产代码**，尤其是：
`apps/web/lib/auth/token-freshness.ts`、`apps/web/lib/auth/dingtalk-inapp-provider.ts`、
以及 `apps/web/app/api/**/route.ts`。若你认为生产代码有问题，
**停下**，把理由写进 handoff.md 并结束，不要自行修改。

## 必须保留的测试意图（不要为了让测试变绿而削弱它）

这 5 个文件里原有的断言是有价值的，**逐条保留**：

- 参数透传（query string → `fetchTimeline` 的入参）
- 校验（空 query → 400；非法枚举值 → 400）
- **RBAC**：`viewer` 不能 `include blocked items`，`admin` 可以

**不要**用"把断言改成期望 401"的方式让测试变绿——那等于删掉了这些用例的全部价值。
正确做法是让 mock 代表一个合法在职用户，使请求能走到被测逻辑。

## 必须新增的测试（至少一条，防止这层保护被静默移除）

在 `timeline-route.test.ts`（或你选定的一个文件）里补一条**反向**用例：
mock 一个**缺 `tokenVersion`** 的用户 → 断言返回 **401**。

这条用例的作用是把 S3b 的安全语义钉住：将来若有人把早返回加回去，这条会变红。
在注释里写明它对应 S3b / T-SEC-06。

## 机械检查（必须执行，结果写进 handoff.md）

1. **确认没有削弱断言**：改完后对比每个文件的断言条数与语义，
   把"改前/改后断言数量"写进 handoff.md。数量不应减少。
2. **确认没碰生产代码**：
   ```bash
   ls -la apps/web/lib/auth/token-freshness.ts apps/web/lib/auth/dingtalk-inapp-provider.ts
   ```
   在 handoff.md 声明未修改这两个文件。

## 验收命令

```bash
cd /Volumes/SD/AI-Timeline-web/apps/web && npx vitest run
cd /Volumes/SD/AI-Timeline-web/apps/web && npx tsc --noEmit -p tsconfig.json
```

**第一条必须是 0 failed**，把原始输出附在最终回复末尾。
