# S10 — 移除 MiroFish 整合（用户决定不再整合）

- executor: `glm`

## ⚠ 本任务禁止 git 提交

不要 `git commit` / `add` / `stash` / `reset` / `checkout <文件>`。改动留工作区。
进度**追加**到根目录 `handoff.md` 末尾（小写），不要改已有内容。

## 背景

用户决定 **MiroFish 不再整合进本项目**，相关代码与部署配置从仓库移除。

这同时消解了一条 High 级安全发现（X-2：`lib/api/mirofish.ts` 的出站 payload 里
`metadata` 与 `documents[].source_url` 未脱敏，手机号 / 内网地址会原样发给公网 LLM）。
**删除代码路径比修脱敏更彻底** —— 原本用于修它的任务 S7 已取消。

## 要做的事

### 1. 删除文件

```
apps/web/app/api/items/[id]/mirofish/route.ts        （整个 mirofish 目录）
apps/web/lib/api/mirofish.ts
apps/web/lib/api/__tests__/mirofish.test.ts
apps/web/components/timeline/mirofish-prediction-button.tsx
```

### 2. 拆掉 2 处 UI 挂载点

- `apps/web/app/items/[id]/page.tsx` —— 第 16 行 import、第 220 行 `<MirofishPredictionButton />`
- `apps/web/components/timeline/item-detail-dialog.tsx` —— 第 9 行 import、第 166 行渲染

**注意 `item-detail-dialog.tsx` 的 `canCreatePrediction` prop**（第 21/25/166 行）：
删掉按钮后该 prop 若无其它用途即成死参数。**先 `grep -rn "canCreatePrediction"` 查清所有传入方**，
一并清掉；若有其它用途则保留并在 handoff.md 说明。不要留下只传不用的 prop。

### 3. `deploy/stack.yml`

- 删 `mirofish` service（第 57–82 行附近）
- 删 `mirofish_uploads` volume（第 310 行附近）
- 删 web 服务上的 `MIROFISH_API_BASE_URL` / `MIROFISH_WEB_BASE_URL` / `MIROFISH_REQUEST_TIMEOUT_MS`（第 25–27 行）

`deploy/compose.portainer-current.yml` **本来就没有 mirofish**，不用改。

### 4. 注释与文档

- `apps/web/lib/api/authz.ts:28` 注释里列举的特权路径含 "mirofish"，去掉该词（**只改注释，不动逻辑**）
- `docs/runbook/external-https-deployment.md:108` 有一条 MiroFish 的 HTTPS 说明，删掉该条

### 5. 保留（不要删）

- `spec/mirofish-seed-integration/` 三份文档 —— 保留作历史记录，用户明确要求保留

## 允许修改的文件（清单外即越界）

```
apps/web/app/api/items/[id]/mirofish/route.ts        （删除）
apps/web/lib/api/mirofish.ts                          （删除）
apps/web/lib/api/__tests__/mirofish.test.ts           （删除）
apps/web/components/timeline/mirofish-prediction-button.tsx （删除）
apps/web/app/items/[id]/page.tsx
apps/web/components/timeline/item-detail-dialog.tsx
apps/web/lib/api/authz.ts
deploy/stack.yml
docs/runbook/external-https-deployment.md
handoff.md
```

其余传入 `canCreatePrediction` 的调用方若在清单外，**停下写进 handoff.md**，不要自行扩范围。

## 机械检查（必须执行，结果写进 handoff.md）

```bash
# 1. 全仓残留引用（应只剩 spec/ 与 tasks/ 下的历史文档）
grep -rn -i "mirofish" --include="*.ts" --include="*.tsx" --include="*.yml" --include="*.md" . \
  | grep -v node_modules | grep -v "\.next" | grep -v "^./spec/" | grep -v "^./tasks/"

# 2. 死 prop 检查
grep -rn "canCreatePrediction" --include="*.tsx" --include="*.ts" apps/web | grep -v node_modules

# 3. YAML 仍可解析
python3 -c "import yaml;yaml.safe_load(open('deploy/stack.yml'));print('stack.yml OK')"
```

第 1 条的输出必须为空（或只剩你无权改的文件，那种情况写进 handoff.md）。

## 需人工执行的动作（写进 handoff.md 单列一节，你不要做）

`deploy/stack.yml` 移除后，Portainer stack 89 上这几个环境变量会变成孤儿，
**且它们是凭据，应当轮换而不只是删除**：
`MIROFISH_LLM_API_KEY`、`MIROFISH_ZEP_API_KEY`、`MIROFISH_SECRET_KEY`、
`MIROFISH_PUBLIC_API_BASE_URL`、`MIROFISH_WEB_BASE_URL`。
另：生产容器 `fe-radar-mirofish-1` 按用户决定**此刻不停**，随下次 stack update 一并消失。

## 验收命令

```bash
cd /Volumes/SD/AI-Timeline-web/apps/web && npx vitest run
cd /Volumes/SD/AI-Timeline-web/apps/web && npx tsc --noEmit -p tsconfig.json
```

两条都必须通过（typecheck 尤其重要——删组件后残留 import 会在这里暴露），原始输出附末尾。
