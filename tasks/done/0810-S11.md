# S11 — 清理 canCreatePrediction 死参数链（S10 遗留）

- executor: `glm`

## ⚠ 本任务禁止 git 提交

不要 `git commit` / `add` / `stash` / `reset` / `checkout <文件>`。改动留工作区。
进度**追加**到根目录 `handoff.md` 末尾（小写），不要改已有内容。
跑命令注意重定向写法用 `2>&1`，不要在仓库留临时文件。

## 背景

S10 移除了 MiroFish 整合，`MirofishPredictionButton` 已删除。
它是 `canCreatePrediction` 这个 prop 的**唯一消费者**——现在该 prop 被一路从页面
穿过 `timeline-list` 传到 `item-detail-dialog`，但没有任何地方使用它。

S10 刻意没清（那 5 个文件在它的允许清单外，它按纪律停下并写进了 handoff.md，这是对的）。
本任务专门做这件事。

## 已定位的 14 处引用

```
apps/web/app/page.tsx:40,63
apps/web/app/search/page.tsx:30,48
apps/web/app/items/page.tsx:46,66
apps/web/components/timeline/timeline-list.tsx:16,21,30,40,45,157
apps/web/components/timeline/item-detail-dialog.tsx:20,24
```

三个页面里的形态都是：

```ts
const canCreatePrediction = hasRole(session?.user?.role, "editor");
...
<TimelineList canCreatePrediction={canCreatePrediction} ... />
```

## 要做的事

删掉整条链：prop 声明、类型定义、透传、以及三个页面里的 `const canCreatePrediction = ...` 赋值。

**注意连带**：删掉赋值后，那三个页面里的 `hasRole` import 可能变成未使用。
**先确认该页面是否还有其它 `hasRole` 调用**——有就保留 import，没有才删。
不要盲删 import（会 typecheck 报错），也不要留未使用 import（lint 会报）。

## ⚠ 不要顺手做的事

- **不要**删 `hasRole` 函数本身或 rbac 模块（其它地方在用）。
- **不要**改动这些页面的其它逻辑、样式、数据获取。
- **不要**因为"看着像死代码"就删别的 prop —— 本任务只针对 `canCreatePrediction`。

## 允许修改的文件（清单外即越界）

```
apps/web/app/page.tsx
apps/web/app/search/page.tsx
apps/web/app/items/page.tsx
apps/web/components/timeline/timeline-list.tsx
apps/web/components/timeline/item-detail-dialog.tsx
handoff.md
```

若发现清单外还有传入方，**停下写进 handoff.md**，不要自行扩范围。
对应测试文件若因 prop 移除而失败，**先在 handoff.md 报告**再决定——不要擅自改测试断言。

## 机械检查（结果写进 handoff.md）

```bash
# 1. 全仓应零命中
grep -rn "canCreatePrediction" --include="*.ts" --include="*.tsx" apps/web | grep -v node_modules

# 2. hasRole 仍被正常使用（确认没误删）
grep -rn "hasRole" --include="*.ts" --include="*.tsx" apps/web | grep -v node_modules | head

# 3. 未使用 import 检查
npx tsc --noEmit -p apps/web/tsconfig.json
```

第 1 条输出必须为空。

## 验收命令

```bash
cd /Volumes/SD/AI-Timeline-web/apps/web && npx vitest run
cd /Volumes/SD/AI-Timeline-web/apps/web && npx tsc --noEmit -p tsconfig.json
```

两条都必须通过，原始输出附末尾。
