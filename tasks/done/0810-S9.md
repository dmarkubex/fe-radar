# S9 — MinIO runbook 不可执行 + compose 与线上不一致（High ×2，X-3 / X-4）

- executor: `glm`

## ⚠ 本任务禁止 git 提交，也禁止改动生产

不要 `git commit` / `add` / `stash` / `reset` / `checkout <文件>`。改动留工作区。
**更不要尝试连 Portainer / MinIO / 生产库做任何操作** —— 本任务只改仓库里的文件。
进度**追加**到根目录 `handoff.md` 末尾（小写）。

---

# 缺陷 A（High）— MinIO runbook 只 attach 不 create，照抄无法执行

`docs/runbook/minio-least-privilege.md:23` / `:43`：直接 `mc admin policy attach` 三个
自定义 policy，**却从未 create 它们**；示例只给了一份 RW JSON；且给 worker 多余的
`GetObject` / `DeleteObject`。

**失败场景**：在干净 MinIO 上照抄步骤 → 三个 policy 不存在 → attach 失败 →
收权部署做不完，旧 root 凭据只能继续用。即使手工建了那份示例 policy，
worker 容器失陷后仍可读取或删除**全部历史简报**，而代码实际只需要"检查 bucket + 上传对象"。

## 要做的事

给出 web / worker / backup **三份精确的 policy JSON**，以及完整的
`mc admin policy create` → `mc admin policy attach` 命令序列。权限按实际代码需要收敛：

- **web**：仅 `s3:GetObject`（下载简报 docx）
- **worker**：仅 `s3:ListBucket` + `s3:PutObject`（检查 bucket + 上传）
- **backup**：可保留 `s3:DeleteObject`（生命周期清理需要）

**先读代码确认每个服务实际调用了哪些 S3 动作**，不要照抄我上面的建议——
我给的是方向，实际权限以代码为准，并把你的核对结果写进 handoff.md。

runbook 还需补：跨 bucket 拒绝、以及"worker 尝试 DeleteObject 应失败"这类**负向验证**步骤。

---

# 缺陷 B（High）— `compose.portainer-current.yml` 与线上不一致，部署会抹掉配置

## ⚠ 主会话已实测，失败方向与首轮评审所述相反，以本节为准

评审报告称该文件"缺 MinIO / 模板 / bucket 配置，会导致简报生成失败"。
主会话直接读了**生产 worker 容器**的环境，实际是：

```
MINIO_ENDPOINT         = minio                                  （线上有）
BRIEFING_TEMPLATE_PATH = /app/design/templates/briefing.docx    （线上有）
BRIEFING_MINIO_BUCKET  = fe-radar-briefings                     （线上有）
FETCH_INTERNAL_ALLOWLIST = <unset>                              （线上确实没有）
```

即 **线上是全的、文件是缺的**。所以真实风险不是"部署后仍缺"，而是
**从这个文件部署会把线上已有的配置抹掉**，简报渲染由好变坏。

## 要做的事

让 `deploy/compose.portainer-current.yml` 与 canonical `deploy/stack.yml` 对齐，
补齐 worker / web 的 MinIO 地址、briefing 模板路径、bucket、以及 scoped secret 引用，
并补上 minio service（若 canonical 有）。**以 stack.yml 为准，不要自创新值。**

`FETCH_INTERNAL_ALLOWLIST: rsshub` 若 S5 已补则确认在位；没有则补。

## ⚠ 这条不要在任务里做（属生产写操作，由人执行）

线上 stack 89 的环境变量本身需要人工补 `FETCH_INTERNAL_ALLOWLIST=rsshub`。
**你只改仓库文件**，并在 handoff.md 里把"需人工在 Portainer 执行的动作"单列一节。

---

## 允许修改的文件（清单外即越界）

```
docs/runbook/minio-least-privilege.md
deploy/compose.portainer-current.yml
deploy/stack.yml
handoff.md
```

## 参考资料（glm 不会主动查文档，以下摘录即权威，按此写命令）

MinIO `mc admin policy` 的正确顺序是**先 create 再 attach**，两条是不同子命令：

```bash
# 1) 用 JSON 文件创建具名 policy（policy 不存在时 attach 会失败）
mc admin policy create <ALIAS> <POLICY_NAME> /path/to/policy.json

# 2) 把已存在的 policy 绑到用户（或 group）
mc admin policy attach <ALIAS> <POLICY_NAME> --user <ACCESS_KEY>
mc admin policy attach <ALIAS> <POLICY_NAME> --group <GROUP>

# 查验
mc admin policy list <ALIAS>
mc admin policy info <ALIAS> <POLICY_NAME>
mc admin user info <ALIAS> <ACCESS_KEY>
```

policy JSON 的基本形状（`Resource` 里 bucket 本身与 bucket 内对象是**两条不同的 ARN**，
`ListBucket` 作用在前者，`GetObject/PutObject/DeleteObject` 作用在后者——写错会导致
"能列不能传"或反之）：

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": ["s3:ListBucket"],
      "Resource": ["arn:aws:s3:::fe-radar-briefings"]
    },
    {
      "Effect": "Allow",
      "Action": ["s3:PutObject"],
      "Resource": ["arn:aws:s3:::fe-radar-briefings/*"]
    }
  ]
}
```

## 机械检查（结果写进 handoff.md）

```bash
# 两份部署文件的环境变量差集，逐条判断是有意差异还是漂移
grep -oE "^\s+[A-Z_]+:" deploy/stack.yml | tr -d ' :' | sort -u > /tmp/a
grep -oE "^\s+[A-Z_]+:" deploy/compose.portainer-current.yml | tr -d ' :' | sort -u > /tmp/b
diff /tmp/a /tmp/b

# 代码实际用到的 S3 动作（用于收敛 policy）
grep -rn "putObject\|getObject\|removeObject\|bucketExists\|listObjects\|statObject" --include=*.ts apps packages | grep -v node_modules
```

## 验收命令

```bash
python3 -c "import yaml,sys; yaml.safe_load(open('deploy/compose.portainer-current.yml')); yaml.safe_load(open('deploy/stack.yml')); print('YAML OK')"
cd /Volumes/SD/AI-Timeline-web/apps/worker && npx vitest run
```

两条都必须通过（第二条含 S5 建立的部署文件镜像一致性测试），原始输出附末尾。
