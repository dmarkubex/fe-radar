I'll fix the project code scrubbing order so longer codes replace first, with no git commits—only the allowed files and `handoff.md`. Reading project state and the scrubber code first.Running the mechanical check and confirming the bug fails before the fix.Confirming the pre-fix failure with a direct script, then applying the sort-by-length fix and tests.Bug confirmed. Applying the length-descending sort and adding the required tests.Revising the nested-code test: a single-letter `"A"` can rematch inside `[REDACTED:...]` placeholders. Using multi-char codes that won't collide with the placeholder template.Appending progress to `handoff.md`.## T15d 完成

### 修法

`scrubText` 在遍历 `projectCodes` 前，对**副本**按字符串长度**降序**排序，再替换；不 mutate 调用方数组。

```44:51:packages/core/src/scrubber.ts
  // Longest codes first so short codes that are substrings of longer ones
  // (e.g. "ZX" vs "ZX-2026") cannot fragment a match and leave residue.
  // Sort a copy — never mutate the caller's projectCodes array.
  const projectCodes = [...(context.projectCodes ?? [])].sort((a, b) => b.length - a.length);
  for (const code of projectCodes) {
    if (code.trim().length === 0) continue;
    cleaned = replaceMatches(cleaned, "PROJECT_CODE", new RegExp(escapeRegExp(code), "g"), found);
  }
```

### 机械检查

`grep projectCodes`：worker 侧只装载/透传，**无遍历顺序假设**；排序只发生在 `scrubber.ts`。

### 回代验算

|            | `projectCodes=["ZX","ZX-2026"]`，text=`项目 ZX-2026 进展顺利`                                           |
| ---------- | ------------------------------------------------------------------------------------------------------- |
| **修复前** | `项目 [REDACTED:PROJECT_CODE:8ddd6efd]-2026 进展顺利` → `-2026: true`，`2026: true`（**红**：碎片残留） |
| **修复后** | `项目 [REDACTED:PROJECT_CODE:ae05eadf] 进展顺利` → `ZX`/`2026`/`-2026` 全 **false**                     |

另补稳定性测：`["FOO-BAR","FOO","FOO-BAR-BAZ"]` 打乱顺序，脱敏后无 `FOO`/`BAR`/`BAZ` 残留（避免单字母代号撞 placeholder 内字母）。

### 范围

本任务只改了：

- `packages/core/src/scrubber.ts`
- `packages/core/src/__tests__/scrubber.test.ts`
- `handoff.md`（进度追加）

**未 commit**（任务禁止 git 写操作）。工作区另有无关改动，回收时勿 `git add -A`。

---

### 验收命令输出

```
cd packages/core && npx vitest run src/__tests__/scrubber.test.ts 2>&1

 RUN  v4.1.5 /Volumes/SD/AI-Timeline-web/packages/core

 Test Files  1 passed (1)
      Tests  6 passed (6)
   Start at  19:54:31
   Duration  176ms (transform 60ms, setup 0ms, import 93ms, tests 2ms, environment 0ms)

cd /Volumes/SD/AI-Timeline-web && npx tsc --noEmit -p packages/core 2>&1
(exit 0, no output)
```
