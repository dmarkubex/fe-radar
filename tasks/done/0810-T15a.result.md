先按任务纪律读项目状态和目标文件；本任务禁止 git 写操作，改动只留工作区并追加 `handoff.md`。工作区里这两个文件已有改动，先看 diff 再决定是接续还是重写。实现三条 High 修复，并补齐覆盖各自失败场景的测试。## T15a 完成（未 commit，改动留工作区）

三条 High 都在 `apps/worker/src/fetchers/playwright.ts` 修完，测试同步更新。

### 修复摘要

| 缺陷               | 修法                                                                                                                                           |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| **1 SSRF 弹窗**    | `installSubresourceGuard` 改挂 `context.route()`；只在 `BrowserContextPool.acquire` **新建 slot** 时装一次；池复用不重装                       |
| **2 listUrl 基准** | `finalUrl = page.url()` 始终取值（不闸 SSRF 开关）→ 传入 `extractItems`；`listUrlNormalized` / `new URL(item.url, base)` 均用 `finalUrl`       |
| **3 池身份**       | `acquire()` 返回 `PooledBrowserContext { context, userAgent, proxy? }`；slot 绑完整 `ProxyEndpoint`；robots / `proxyPool.release` 用返回值身份 |

### 新增/针对性测试

1. context 级 route 捕获 + `page.route` 调用次数为 0
2. 池复用时 `context.route` 只装 2 次（= MAX_CONTEXTS）
3. 重定向 + 浏览器解析后的空 href → 过滤 / 全无效抛 `FETCH_PLAYWRIGHT_EMPTY`
4. 旧 listUrl 基准会放行 vs 新 finalUrl 基准会过滤（回代用例）
5. 池满后返回 slot 绑定的 UA/proxy，不是本次请求值

### 机械检查

- `context.route` 安装点：`:211`（仅此一处 `.route(` 调用）
- `acquire(`：`pool.acquire` 定义 + `proxyPool.acquire` + `pool.acquire(...)` 解构适配
- 范围：仅改上述两源文件（`handoff.md` 追加进度，且被 gitignore）

### 回代验算（缺陷 2）

```
listUrl:  https://x.com/start
finalUrl: https://x.com/final/
item.url: https://x.com/final/   # 空 href 浏览器解析结果

旧逻辑: listUrlNormalized=https://x.com/start, resolved=https://x.com/final/ → kept=true  (放行垃圾)
新逻辑: listUrlNormalized=https://x.com/final/, resolved=https://x.com/final/ → kept=false (正确过滤)
RESULT: PASS
```

---

### 验收命令输出

```text
cd apps/worker && npx vitest run src/fetchers/__tests__/playwright.test.ts 2>&1

 RUN  v4.1.5 /Volumes/SD/AI-Timeline-web/apps/worker

{"level":40,...,"url":"http://127.0.0.1:8080/admin","reason":"PORT_BLOCKED:8080","msg":"playwright subresource blocked by SSRF guard"}
{"level":40,...,"url":"http://169.254.169.254/latest/meta-data","reason":"METADATA_IP_BLOCKED","msg":"playwright subresource blocked by SSRF guard"}

 Test Files  1 passed (1)
      Tests  16 passed (16)
   Duration  276ms
```

```text
cd /Volumes/SD/AI-Timeline-web && npx tsc --noEmit -p apps/worker 2>&1
(exit 0, no errors)
```
