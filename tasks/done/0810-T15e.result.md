按 T15e 范围实现：先读 `AI_index.md`、`handoff.md` 与相关 seed/测试先例，再改 `upsertAdmin` 并补测试。正在实现：导出 `upsertAdmin`、按 `bcrypt.compare` 条件递增 `token_version`，并加上与 purge 脚本一致的 main 守卫。验收已通过。正在把进度追加到 `handoff.md`。## T15e 完成

**范围**：仅改/建

- `packages/db/scripts/seed-admin.ts`
- `packages/db/src/__tests__/seed-admin.test.ts`
- 进度追加到 `handoff.md`（gitignore，不进 git）

**禁止 git 写操作**：未 `add` / `commit`。

### 改动要点

1. **`upsertAdmin` 导出**，可被单测 import；`main()` 仅在 `argv` 含 `seed-admin` 时执行（与 purge 脚本同一模式）。
2. 先 `SELECT password_hash`；有存量则用 **`bcrypt.compare(明文, 旧hash)`** 判 `isChanged`（不用 SQL 比 hash）。
3. **`token_version` 条件递增**：
   - `isChanged === true` → `users.token_version + 1`
   - `isChanged === false` → `users.token_version`（保持）
4. `name` / `dept` / `role` 仍无条件更新。

### 机械检查

```
grep -n "token_version" packages/db/scripts/seed-admin.ts
→ 50–51: isChanged ? sql`users.token_version + 1` : sql`users.token_version`
→ 61: token_version = ${tokenVersionExpr}
```

### 回代验算

| 场景       | 修复前                                          | 修复后                         |
| ---------- | ----------------------------------------------- | ------------------------------ |
| A 首次创建 | DEFAULT 0 + 视为变化                            | 同左，SQL 含 `+ 1`             |
| B 密码未变 | **每次 +1（踢会话）**；旧片段无法通过 keep 断言 | **保持** `users.token_version` |
| C 密码真变 | +1                                              | +1（T-SEC-06 不回归）          |

### 验收输出（原样）

```
 RUN  v4.1.5 /Volumes/SD/AI-Timeline-web/packages/db

 Test Files  1 passed (1)
      Tests  8 passed (8)
   Start at  19:58:28
   Duration  3.57s (transform 16ms, setup 0ms, import 27ms, tests 3.46s, environment 0ms)
```

`npx tsc --noEmit -p packages/db` → **0 error**（exit 0）。
