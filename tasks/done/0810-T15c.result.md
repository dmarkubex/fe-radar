按 T15c 执行：先读内核状态与相关源码，确认 T15a 的 `finalUrl` 修法与 `verify-sources` 当前实现。继续读 T15a 的 `finalUrl` 实现与现有测试，准备镜像修复。开始改三个文件：声明 `playwright` 依赖、镜像 `finalUrl` 修法，并补回代测试。接下来跑 `pnpm install` 和验收命令。`pnpm install` 因 frozen-lockfile 失败，改用不冻结 lockfile 重装。类型错误来自内联 `Browser` 与真实 Playwright 类型不兼容；改为用 Playwright 的正式类型。改用 Playwright 的 `Browser` 类型，去掉不兼容的内联类型。验收通过。正在把进度追加到 `handoff.md` 末尾。## T15c 完成（改动留工作区，未做任何 git 写操作）

### 已完成

1. **缺陷 1**：`packages/db/package.json` 的 `devDependencies` 增加 `"playwright": "^1.59.1"`；`pnpm install --no-frozen-lockfile` 更新了 `pnpm-lock.yaml`；去掉 `@ts-expect-error`。
2. **缺陷 2**：`verify-sources.ts` 在 `page.goto` 后捕获 `const finalUrl = page.url()`，`$$eval` 的 `listUrl` 改为 `finalUrl`（镜像 T15a）。
3. **类型**：声明依赖后内联 `browser` 类型与真实 Playwright `Page.goto` 不兼容；改为 `import type { Browser } from "playwright"`，**未**用 `as any`。
4. **测试**：既有 mock 补 `url()`；新增 T15c 重定向空 href 集成测 + 纯单元回代。
5. **handoff.md** 已追加 T15c 进度。

### 范围核对（补集）

本任务触及：

- `packages/db/package.json`
- `packages/db/scripts/verify-sources.ts`
- `packages/db/src/__tests__/verify-sources.test.ts`
- `pnpm-lock.yaml`
- `handoff.md`（进度）

**未改** `apps/worker/src/fetchers/playwright.ts`。

### 判定基准语义一致（机械检查第 4 点）

两边都用**导航后真实落点**做空 href 回退基准：worker `playwright.ts` 用 `const finalUrl = page.url()` → `extractItems(..., finalUrl)` → 归一化 `finalUrl` 再比；`verify-sources.ts` 同样 `const finalUrl = page.url()` → `listUrl: finalUrl` 传入 `$$eval` 内联判定。都不用配置里的原始 `targetUrl`/`config.listUrl`。

### 回代验算

| 输入                                                                                        | 修复前 (`listUrl=targetUrl`)                           | 修复后 (`listUrl=finalUrl`)                                                            |
| ------------------------------------------------------------------------------------------- | ------------------------------------------------------ | -------------------------------------------------------------------------------------- |
| `targetUrl=https://x.com/start`，`finalUrl=https://x.com/final/`，空 href 解析为 `finalUrl` | `oldValid=true` → matched=1, valid=1 → 门禁放行（bug） | `newValid=false` → matched=1, valid=0 → `{ ok: false, error: ...empty title or link }` |
| 集成测 `T15c: fails empty-href...`                                                          | —                                                      | `ok:false`；`$$eval` 的 `listUrl === finalUrl`                                         |

---

### 验收命令输出（原样）

```
Scope: all 7 workspace projects
Already up to date
Done in 140ms using pnpm v11.0.9
```

```
 RUN  v4.1.5 /Volumes/SD/AI-Timeline-web/packages/db

 Test Files  1 passed (1)
      Tests  49 passed (49)
   Start at  19:51:37
   Duration  295ms (transform 56ms, setup 0ms, import 220ms, tests 12ms, environment 0ms)
```

```
(tsc --noEmit -p packages/db → exit 0, 无错误输出)
```
