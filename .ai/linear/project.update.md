# Linear Project Update — FE-Radar

> Per-issue progress log produced by CCB agents before stop, per `.ai/current-linear.md` Exit Rule.

**Update Window**：2026-05-19 CST · Claude Code Plan-Fix（v1.1 Commodity Briefing v0.3 → v0.4）
**Scope**：Linear Project [FE-Radar — 远东控股行业情报雷达](https://linear.app/dmarkubex/project/fe-radar-远东控股行业情报雷达-9536558a9480)

---

## Per-Issue Progress

### [DMA-46](https://linear.app/dmarkubex/issue/DMA-46) REVIEW-C1+C2 (mock-mode security R2)

- **Before**: R2 fix was staged in the dirty worktree but not committed separately.
- **Action**: Codex created isolated commit `f22464e [DMA-46] 加固 mock-mode 双层 NODE_ENV 护栏 + cached bcrypt hash`.
- **Changed files in commit**:
  - `apps/web/lib/mock-mode.ts`
  - `apps/web/lib/auth/users.ts`
  - `apps/web/lib/__tests__/mock-mode.test.ts`
  - `apps/web/lib/auth/__tests__/users.test.ts`
- **Verification evidence**:
  - `grep -R "NEXT_PUBLIC_APP_DATA_MODE" apps packages || true` returned no matches.
  - `git diff --check --cached` passed.
  - `git commit` completed with the configured pre-commit hook.
- **Verification blocked**:
  - Targeted web tests and web typecheck were attempted with `CI=true`, but pnpm dependency-status check recreated `node_modules` and timed out while downloading `next` / `@next/swc-darwin-arm64` tarballs. No test/typecheck assertion failure was reached.
- **Uncommitted local cleanup**:
  - `.gitignore` now has an uncommitted change for `/AGENTS.md` and `.ccb*` ignores.
  - Local untracked `AGENTS.md`, `.ccb/`, `.ccb-backups/`, and `.ccb-requests/` were removed before the human pause instruction arrived.
- **Next owner**: Claude Code. Codex stopped per human instruction and did not continue worktree commit splitting.

### [DMA-153](https://linear.app/dmarkubex/issue/DMA-153) V11-REVIEW (Antigravity Plan Review)

- **Before**：Todo · Antigravity 已交回 APPROVED-with-conditions（3 Minor + 4 Edge），等 Claude Code Plan-Fix。
- **Action**：Plan-Fix 闭合 7 项 findings → spec v0.4。
- **Changed files**：
  - `spec/v1.1-commodity-briefing/design.md` (v0.3 → v0.4)
  - `spec/v1.1-commodity-briefing/tasks.md` (v0.3 → v0.4)
- **Closure table**：
  - M1 `template_version` → design §7.1 添加 `INT NOT NULL DEFAULT 1` + 索引
  - M2 `briefing_targets.disabled_at` → design §7.1 添加 `TIMESTAMPTZ` + partial index
  - M3 quotes upsert Tier 优先级 → tasks T-CB-09 加 `WHERE EXCLUDED.tier <= current.tier` + 3 个 acceptance 测试
  - E1 briefing-gen/quotes-fetch 队列竞态 → design §5.2 step 1 + tasks T-CB-13 加 queue.getJobCounts 检查 + abort 路径
  - E2 raw_text 单位歧义 → 统一 **2000 字符**（design §7.1 / requirements §9.2 / tasks T-CB-01 三处口径已对齐）
  - E3 computeSupportResistance 样本不足静默 → design §6.5 + tasks T-CB-17 详情页 UI 降级提示条 + fixture acceptance
  - E4 seed 幂等 → tasks T-CB-03 显式约定 seed 仅首次初始化 + migration 序号即 seed_version 审计
- **Verification**：Antigravity 报告 §5 第 3 条明示「无需二轮」（findings 不涉及架构重构）；report 原件 `.ai/linear/v11-plan-review-report.md`。
- **Next state**：Done（人工标 Done · 再无 Claude Code 动作）。

### [DMA-154](https://linear.app/dmarkubex/issue/DMA-154) V11-P1 (Execute Phase 1)

- **Before**：Backlog · blockedBy v1.0.0 release tag。
- **Action**：本轮无直接修改，但 spec v0.4 已成为 Execute 基线（design/tasks 同步至 v0.4）。
- **Next state**：保持 Backlog · v1.0.0 release tag 推到内网 registry 后人工升 Todo 交 Codex。

### DMA-151 / DMA-152 / DMA-155..178

- 无变化。28 issue 中 DMA-152 + DMA-153 已 Done；其余 26 仍 Backlog；39 条 blockedBy 完整。

---

## Proposed New Issues

无。v0.4 fix 全部走 spec 增量，无需独立 Linear issue。

---

## Blockers

- **v1.1 Execute 仍 blockedBy v1.0.0 release tag**（详见根 `handoff.md §8 Release Plan v1.0.0` Phase 1–5）。v1.1 不在 v1.0 release 期启动。

---

## Verification

- 文件可达性：`spec/v1.1-commodity-briefing/design.md` + `spec/v1.1-commodity-briefing/tasks.md` 已就地编辑（Edit 工具确认成功）。
- 内化校验：
  - design.md header v0.4 + v0.3→v0.4 changelog 7 条
  - tasks.md header v0.4 + v0.3→v0.4 changelog 7 条
  - 7 个 findings 锚点均含 `v0.4 fix Mx/Ex` 标记，便于评审 grep
- v1.0.0 release stream 完全未触：未改 `apps/**` / `packages/**` / `deploy/**` / 任何 v1.0 spec。

---

*Generated by Claude Code · 2026-05-19 CST · v1.1 Plan-Fix close*
