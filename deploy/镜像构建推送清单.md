# 镜像构建推送清单

> 适用位置：在构建服务器的项目根目录执行，例如 `/Volumes/SD/AI-Timeline-web`。
> 本清单构建并推送 FE-Radar 自建镜像 + 镜像公共镜像到内网 Harbor（部署服务器无法访问 Docker Hub）。

## 1. 登录 Harbor

```bash
docker login harborssl.fegroup.cn
```

## 2. 设置镜像仓库变量

这行可以单独执行。后续命令必须在同一个 shell 窗口里继续执行，`$R` 才会生效。

```bash
R=harborssl.fegroup.cn/custom-project
echo "$R"
```

`echo "$R"` 应输出：

```bash
harborssl.fegroup.cn/custom-project
```

## 3. 构建自建镜像

```bash
docker build -f deploy/Dockerfile.postgres-zhparser -t "$R/fe-radar/postgres-zhparser:pg16" .
docker build -f deploy/Dockerfile.worker            -t "$R/fe-radar-worker:latest" .
docker build -f deploy/Dockerfile.migrate           -t "$R/fe-radar-migrate:latest" .
docker build -f deploy/Dockerfile.web               -t "$R/fe-radar-web:latest" .
docker build -f deploy/Dockerfile.backup            -t "$R/fe-radar-backup:latest" .
```

## 4. 推送自建镜像

```bash
docker push "$R/fe-radar/postgres-zhparser:pg16"
docker push "$R/fe-radar-worker:latest"
docker push "$R/fe-radar-migrate:latest"
docker push "$R/fe-radar-web:latest"
docker push "$R/fe-radar-backup:latest"
```

## 5. 对应服务

| 镜像                              | 用途                              |
| --------------------------------- | --------------------------------- |
| `fe-radar/postgres-zhparser:pg16` | Postgres 16 + pgvector + zhparser |
| `fe-radar-worker:latest`          | worker + scheduler 共用镜像       |
| `fe-radar-migrate:latest`         | 一次性 migration / seed 镜像      |
| `fe-radar-web:latest`             | Next.js web / API                 |
| `fe-radar-backup:latest`          | MinIO backup / provisioning       |

## 6. 镜像公共镜像到 Harbor

部署服务器无法直连 Docker Hub，需在构建机（能联网）上拉取公共镜像并推到 Harbor：

```bash
docker pull redis:7-alpine
docker tag redis:7-alpine "$R/redis:7-alpine"
docker push "$R/redis:7-alpine"

docker pull minio/minio:RELEASE.2025-09-07T16-13-09Z
docker tag minio/minio:RELEASE.2025-09-07T16-13-09Z "$R/minio:RELEASE.2025-09-07T16-13-09Z"
docker push "$R/minio:RELEASE.2025-09-07T16-13-09Z"

docker pull grafana/grafana:12.1.1
docker tag grafana/grafana:12.1.1 "$R/grafana:12.1.1"
docker push "$R/grafana:12.1.1"

docker pull diygod/rsshub:latest
docker tag diygod/rsshub:latest "$R/rsshub:20260615"
docker push "$R/rsshub:20260615"
```

## 7. 对照表（全部 9 个镜像）

| Harbor 路径                          | 来源     | 用途                              |
| ------------------------------------ | -------- | --------------------------------- |
| `fe-radar/postgres-zhparser:pg16`    | 自建     | Postgres 16 + pgvector + zhparser |
| `fe-radar-worker:latest`             | 自建     | worker + scheduler 共用           |
| `fe-radar-migrate:latest`            | 自建     | 一次性 migration / seed           |
| `fe-radar-web:latest`                | 自建     | Next.js web / API                 |
| `fe-radar-backup:latest`             | 自建     | MinIO backup / provisioning       |
| `redis:7-alpine`                     | 公共镜像 | Redis 缓存 / BullMQ               |
| `minio:RELEASE.2025-09-07T16-13-09Z` | 公共镜像 | 对象存储（简报 docx）             |
| `grafana:12.1.1`                     | 公共镜像 | 监控仪表盘                        |
| `rsshub:20260615`                    | 公共镜像 | 内网 RSS 源代理                   |
