# AI harness / agent local state（非项目资产）
/.ai/
/.antigravitycli/
/handoff.md

# 系统文件
.DS_Store

# OMC 本地状态（每会话生成，非项目资产）
/.omc/

# 本地 agent / CCB 运行态（非项目资产）
/AGENTS.md
/.ccb/
/.ccb-backups/
/.ccb-requests/

# 设计稿 / 原型 HTML+PNG（参考资产，避免膨胀仓库）
# 用 /design/* 而非 /design/ 以便 git 仍能下钻、对下方例外生效
/design/*
# 例外：v1.1 简报 docx 模板是生产资产，必须纳入 git（CLAUDE.md 陷阱 #10 / T-CB-11）
!/design/templates/
/design/templates/*
!/design/templates/briefing.docx

# Node 依赖与早期临时实验产物（M0 monorepo 起步时按 spec/tasks.md T-M0-01 重新建立）
/node_modules/
**/node_modules/
/package-lock.json
/.pnpm-store/
*.tsbuildinfo
coverage/
**/coverage/
test-results/
**/test-results/
playwright-report/
**/playwright-report/
dist/
**/dist/

# KYO-54 embedding benchmark datasets (regenerate via scripts/seed/generate-embedding-dataset.ts)
generated/embedding/
.next/
**/.next/

# 凭据 / 密钥（推送到 GitHub 前的硬化 · 任何 .env / 私钥 / 证书都不进版本库）
.env
.env.*
!.env.example
*.pem
*.key
*.crt
*.p12
*.pfx
